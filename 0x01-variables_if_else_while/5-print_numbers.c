#include <stdio.h>

/**
 * main - Entry point
 * Description : printnumbers
 * Return: always 0 (Success)
*/

int main(void)
{
	int number = 0;

	while (number <= 9)
	{
		printf("%i", number);
	}
	printf("\n");

	return (0);
}
